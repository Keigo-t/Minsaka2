<h3>投稿一覧</h3>

<% @posts.each do |post| %>
  <%= link_to admin_user_path(post.user.id) do %>
    <%= post.user.name %>
  <% end %>
  <%= link_to admin_post_path(post.id) do %>
    <%= post.post %>
  <% end %>
<% end %>